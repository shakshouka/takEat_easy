<div class="container">
  <h1>Connexion</h1>
<br>

<div id="home-informations">
  <h2>Pour entrer sans créer un nouveau compte, utiliser les éléments suivants:
    <ul>
      <li>Identifiant: toto1@yahoo.fr</li>
      <li>Mot de passe: abcdef </li>
    </ul>
  </h2>
  <br>
  <h2>Ce site est optimisé pour une navigation sur <strong> Iphone X</strong>, pour voir une démonstration de l'app, c'est par <u><a href="https://youtu.be/BKkxUFBYtVU?t=1243" target="blank">ici</a></u> (entre 20'40''' et 25'20'').</h2>
  <br>
</div>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" }, label: 'Adresse de messagerie'  %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" }, label: 'Mot de passe' %>
      <%= f.input :remember_me, label: 'Se souvenir de moi', as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Connexion", class: "btn-main", style:"width: 100%; margin-bottom:30px" %>
    </div>
  <% end %>

<%= render "devise/shared/links" %>
</div>
